[tool.poetry]
package-mode = false
readme = 'README.md'

[tool.poetry.dependencies]
python = "^3.12.2"
python-dotenv = "^1.0.1"
WMI = "^1.5.1"
colorlog = "^6.8.2"
pyserial = "^3.5"

[build-system]
requires = ["poetry-core>=0.9.0"]
build-backend = "poetry.core.masonry.api"
